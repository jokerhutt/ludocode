---
title: Enabling Features
slug: enabling-features
section: Getting Started
order: 1
description: Enabling optional features such as authentication, AI, and code execution.
---

## Optional Feature Configuration

Without additional configuration, Ludocode runs with authentication, AI, and code execution disabled. The sections below explain how to enable each feature.

<Tabs
  tabs={[
{
  label: "Firebase Authentication",
  content: (
    <div className="text-sm text-ludo-white space-y-5">

      <p className="font-semibold text-ludo-white-bright">1. Create a Firebase Project</p>

      <p>
        Go to <code>https://console.firebase.google.com</code> → Create Project → Open the dashboard.
      </p>

      <p className="font-semibold text-ludo-white-bright">2. Enable Authentication Providers</p>

      <p>
        Navigate to <code>Authentication → Sign-in method</code> and enable:
      </p>

      <ul className="list-disc ml-5 space-y-1">
        <li>Google</li>
        <li>GitHub</li>
        <li>Email/Password</li>
      </ul>

      <p>
        Under <code>Authentication → Settings → Authorized domains</code>, add:
      </p>

      <CodeBlock language="bash">{`localhost

your-production-domain.com`}</CodeBlock>

      <p className="font-semibold text-ludo-white-bright">3. Configure Frontend Environment (Required)</p>

      <p>
        Add the following to both:
      </p>

      <ul className="list-disc ml-5">
        <li><code>apps/web/.env</code></li>
        <li><code>apps/admin/.env</code></li>
      </ul>

      <CodeBlock language="bash">{`VITE_FIREBASE_API_KEY=

VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_APP_ID=`}</CodeBlock>

      <p>
        These values come from <code>Project Settings → General → Your Apps</code>.
      </p>

      <p className="font-semibold text-ludo-white-bright">4. Configure OAuth Origins</p>

      <p>Add to your OAuth provider settings:</p>

      <p className="text-ludo-white-bright">Authorized JavaScript origins</p>

      <CodeBlock language="bash">{`http://localhost:5173

http://localhost:5174
https://your-frontend-domain.com`}</CodeBlock>

      <p className="text-ludo-white-bright">Authorized Redirect URIs</p>

      <CodeBlock language="bash">{`http://localhost:5173

http://localhost:5174
https://your-frontend-domain.com`}</CodeBlock>

      <p className="font-semibold text-ludo-white-bright">5. Configure Backend Service Account</p>

      <p>
        Go to <code>Project Settings → Service Accounts</code> → Generate new private key.
      </p>

      <CodeBlock language="bash">{`FIREBASE_SERVICE_ACCOUNT_JSON={...full JSON content...}`}</CodeBlock>

      <Tip variant="warning" title="Important">
        If either frontend Firebase variables or backend service account JSON are missing,
        authentication will be disabled and the app falls back to demo mode.
      </Tip>

        <p className="font-semibold text-ludo-white-bright">6. Enable Firebase auth in the backend environment variables</p>

        <CodeBlock language="bash">{`FIREBASE_AUTH=true`}</CodeBlock>

    </div>

),
},

    {
      label: "AI Chatbot (Gemini)",
      content: (
        <div className="text-sm text-ludo-white space-y-5">

          <p className="font-semibold text-ludo-white-bright">1. Generate Gemini API Key</p>

          <p>
            Visit <code>https://aistudio.google.com</code> → Sign in → Create API Key.
          </p>

          <p className="font-semibold text-ludo-white-bright">2. Configure Backend Environment</p>

          <CodeBlock language="bash">{`AI_ENABLED=true

GEMINI_API_KEY=your_api_key_here`}</CodeBlock>

          <Tip variant="info" title="Optional">
            If <code>AI_ENABLED</code> is not set to true, AI features remain disabled.
          </Tip>

        </div>
      ),
    },

    {
      label: "Code Runner",
      content: (
        <div className="text-sm text-ludo-white space-y-5">

          <Tip variant="warning" title="Required">
            Ensure you have a Linux environment, Docker, Node JS 15 or later, cgroup v2 enabled, and cgroup v1 disabled
          </Tip>

          <p className="font-semibold text-ludo-white-bright">1. Clone the Piston repo</p>

          <CodeBlock language="bash">
            {`# clone and enter repo

git clone https://github.com/engineer-man/piston`}

</CodeBlock>

          <p className="font-semibold text-ludo-white-bright">2. Start the API container & install the CLI</p>

          <CodeBlock language="bash">
            {`# Start the API container

docker-compose up -d api

# Install all the dependencies for the cli

cd cli && npm i && cd -`}

</CodeBlock>

          <p className="font-semibold text-ludo-white-bright">3. Install the runtimes you want (with Python example)</p>
          <CodeBlock language="bash">
            {`# List all available packages

cli/index.js ppman list

# Install latest python

cli/index.js ppman install python

# Install specific version of python

cli/index.js ppman install python=3.9.4`}

</CodeBlock>

          <p className="font-semibold text-ludo-white-bright">3. Set the Piston URL on the backend & enable Piston</p>
          <CodeBlock language="bash">
            {`# Set the URL

PISTON_BASE=http://localhost:2000/api/v2

# Enable Piston Runtime

PISTON_ENABLED=true

`}

</CodeBlock>

<hr />

          <Tip variant="info" title="Issues?">
            For more in depth instructions on setting up Piston, see their repository page <a>https://github.com/engineer-man/piston</a>
          </Tip>


        </div>

      ),
    },

]}
/>
